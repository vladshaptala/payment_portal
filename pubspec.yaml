name: payment_portal
description: "Hyper-Tenant Secure Payment Portal — multi-brand white-label engine with Android native security."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2 
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  collection: ^1.18.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  riverpod_generator: ^4.0.3 
  freezed: ^3.2.5
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
